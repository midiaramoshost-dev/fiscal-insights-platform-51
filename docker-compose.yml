version: '3.8'

services:
  fiscal-insights-web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - coolify-network

networks:
  coolify-network:
    external: true
